
// This function handles the response data and determines its status. 
// If the response is already a StatusRequest, it returns it directly. 
// Otherwise, it assumes the request was successful and returns StatusRequest.success.
/*
import 'package:flutter/material.dart';
import 'package:phoinex/core/class/statudrequest.dart';

handlingdata(response){
  if(response is StatusRequest){
    return response;
    }
  
  else{
    return StatusRequest.success;
  }
}
*/